projects:
  - name: default
    path: .
    terraform_working_dir: .
    workflow: default

workflows:
  default:
    plan:
      steps:
        - run: terraform init
        - run: terraform plan
      env:
        GITHUB_CONTEXT: ${{ toJson(github) }}
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}